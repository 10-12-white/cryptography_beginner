	# lovasc condition and then go from there
	def check_lovasc(b, b1, mu):
		basis_vectors = []
		k = np.round(mu)
		b_reduced = b - k* b1
		mu_reduced =  mu - k
		if (b_reduced * delta)^2 <= gmcoef^2 * b1-1^2 + bi^2:
			basis_order = basis_vectors
		return true

# original helper function
